import classes from './Instructions.module.css';

const Instructions = () => {
  return (
    <div className={classes.title}>
      <h2>Instructions</h2>
      <p>
        Choose difficulty between EASY, NORMAL, HARD and then select blablabla
      </p>
    </div>
  );
};

export default Instructions;
